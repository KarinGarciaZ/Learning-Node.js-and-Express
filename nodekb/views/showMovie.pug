extends layout

block content
  h1= movie.title    
  h4 Type: #{movie.type}
  h2 Price: $ #{movie.price}
  hr
  a.btn.btn-default(href='/movies/edit/'+movie._id) Edit
  a.btn.btn-danger.delete-movie(href='/movies/delete/', data-id=movie._id) Delete